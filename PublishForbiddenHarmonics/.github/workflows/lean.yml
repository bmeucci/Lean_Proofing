name: Lean Build
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install elan
        run: |
          curl -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y
          echo "$HOME/.elan/bin" >> $GITHUB_PATH

      - name: Cache .lake
        uses: actions/cache@v4
        with:
          path: .lake
          key: lake-${{ hashFiles('lean-toolchain', 'lake-manifest.json') }}
          restore-keys: lake-

      - name: Fetch Mathlib cache
        run: lake exe cache get || true

      - name: Build
        run: lake build

      - name: Verify no sorry in main theorem
        run: |
          lake env lean -c 'import ForbiddenHarmonics.Synthesis
          #print axioms main_theorem' 2>&1 | \
          grep -v sorryAx && echo "✓ No sorry axioms found" || \
          (echo "✗ sorryAx detected!" && exit 1)
